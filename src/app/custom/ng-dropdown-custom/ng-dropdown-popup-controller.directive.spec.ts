import { NgDropdownPopupControllerDirective } from './ng-dropdown-popup-controller.directive';

describe('NgDropdownPopupControllerDirective', () => {
  it('should create an instance', () => {
    const directive =new NgDropdownPopupControllerDirective();
    expect(directive).toBeTruthy();
  });
});
