import { CustomSlicePipePipe } from './custom-slice-pipe.pipe';

describe('CustomSlicePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSlicePipePipe();
    expect(pipe).toBeTruthy();
  });
});
